# section information
section:
  name: Posts
  id: recent-posts
  enable: true
  weight: 5
  showOnNavbar: false
  # Can optionally hide the title in sections
  hideTitle: true

# no additional configuration is required
